#include <stdio.h>
int a[] = {10, 3, 5, -8, 9 , 0 , 33, 60};
#define SWAP(A, B) do { typeof(A) t; \
    t = (A);                         \
    (A) = (B);                       \
    (B) = t;                         \
  } while(0)

void bubble(int *array, int size) {
  int i, j;
  for (i = 1; i < size; i++) {
    for(j = i-1; j > 0; j--) {
      if(array[i] )
    }
  }
}


int main() {


}
