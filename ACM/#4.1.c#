#include <stdio.h>
#include <string.h>

int main() {
     char buf[100];

     scanf("%s", buf);

     int len = strlen(buf);

     int i;

     for(i=0; i<len; i++) {
          if(
     }
     return 0;

